"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (_) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
Object.defineProperty(exports, "__esModule", { value: true });
var needle = require("needle");
var fs = require("fs");
var analytics = require("../analytics");
var debugModule = require("debug");
var proxyFromEnv = require("proxy-from-env");
var ProxyAgent = require("proxy-agent");
var debug = debugModule('snyk:fetch-patch');
var getProxyForUrl = proxyFromEnv.getProxyForUrl;
function getPatchFile(patchUrl, patchFilename) {
    return __awaiter(this, void 0, void 0, function () {
        var options, proxyUri, res, patchData, error_1;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    proxyUri = getProxyForUrl(patchUrl);
                    if (proxyUri) {
                        debug('Using proxy: ', proxyUri);
                        options = { agent: new ProxyAgent(proxyUri) };
                    }
                    _a.label = 1;
                case 1:
                    _a.trys.push([1, 3, , 4]);
                    return [4 /*yield*/, needle('get', patchUrl, options || {})];
                case 2:
                    res = _a.sent();
                    if (!res || res.statusCode !== 200) {
                        throw res;
                    }
                    patchData = res.body;
                    debug("Successfully downloaded patch from " + patchUrl + ", patch size " + patchData.length + " bytes");
                    return [3 /*break*/, 4];
                case 3:
                    error_1 = _a.sent();
                    debug("Failed to download patch from " + patchUrl, error_1);
                    analytics.add('patch-fetch-fail', {
                        message: error_1 && (error_1.message || error_1.body),
                        code: error_1 && error_1.statusCode,
                    });
                    throw error_1;
                case 4:
                    try {
                        fs.writeFileSync(patchFilename, patchData);
                        debug("Successfully wrote patch to " + patchFilename);
                    }
                    catch (error) {
                        debug("Failed to write patch to " + patchFilename, error);
                        analytics.add('patch-fetch-fail', {
                            message: error && error.message,
                            patchFilename: patchFilename,
                            patchUrl: patchUrl,
                        });
                        throw error;
                    }
                    return [2 /*return*/, patchFilename];
            }
        });
    });
}
module.exports = getPatchFile;
//# sourceMappingURL=fetch-patch.js.map