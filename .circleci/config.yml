version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10
    steps:
      - checkout
      - run:
          name: Install yarn package manager
          command: curl -o- -L https://yarnpkg.com/install.sh | bash
      - restore_cache: # special step to restore the dependency cache
          keys:
            - yarn-packages-{{ checksum "./client/yarn.lock" }}
            - yarn-packages-
      - run:
          name: Install Dependencies
          command: cd client && yarn install --frozen-lockfile
      - save_cache:
          name: Save Yarn Package Cache
          paths:
            - ~/.cache/yarn
          key: yarn-packages-{{ checksum "./client/yarn.lock" }}
      - run:
          name: Make coverage directory for test results
          command: cd client && mkdir client-test-results
      - run: # run tests
          name: Run Test and Coverage
          command: |
            cd client
            yarn run test:ci
      - store_test_results:
          path: ~/client/client-test-results
      - run:
          name: Compile react build
          command: cd client && yarn run build
